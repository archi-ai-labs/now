# OS
.DS_Store

# Editors
.idea/
.vscode/

# Claude Code local state (never commit machine-specific settings)
.claude/settings.local.json

# Logs / temp
*.log
node_modules/

# NOW board — local working state, per machine. This plugin writes these two
# files into whatever repo it runs in, including its own.
NOW.json
NOW.md
